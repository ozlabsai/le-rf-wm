train_path: ${oc.env:RF_DATA_DIR,/workspace/data}/train.h5
val_path: ${oc.env:RF_DATA_DIR,/workspace/data}/val.h5
history_size: ${wm.history_size}
num_preds: ${wm.num_preds}
normalize: True  # global per-channel mean/std from train split
